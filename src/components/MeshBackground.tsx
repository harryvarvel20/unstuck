/**
 * The static background material (Old Money): a whisper of aged gold over
 * the ivory paper, plus a barely-there linen grain. No motion. Styled by the
 * `.mesh` rules in globals.css.
 */
export function MeshBackground() {
  return <div className="mesh" aria-hidden="true" />;
}
